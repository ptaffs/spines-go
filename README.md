# spines-go
version of spines in GO language which reduces dependencies on all sorts of things, but mainly Apache httpd and then installing the correct files in the correct folders, since Golang has API handlers and asset serving.
